<%= form_for @bookmark do |f| %>

  <p>
    <%= f.label :list, "New list name:" %><br />
    <%= f.text_area :list %>
  </p>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <p><%= f.submit "Create list" %></p>
<% end %>