<%= form_for(@category) do |f| %>

      <%= f.select(:id, 
      (@categories),  prompt: "categories") %>

            <%= f.select(:subcategory_id, 
      (@subcategories),  prompt: "subcategories") %>

      <%= f.submit "Create new product", categories_path %>
<% end %>